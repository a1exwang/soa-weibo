<p id="notice"><%= notice %></p>

<h1>Current User: <%= current_user.id %></h1>
<h2>需要先注册并且登陆才能使用瞄~😉</h2>
<% if current_user && current_user.token %>
    <%= link_to 'Test Emotion', emotion_path(current_user.id), method: :get %>
    <br/>
    <%= link_to 'Logout', destroy_user_session_path, method: :delete %>
<% else %>
    <%= link_to 'Sina Weibo',
                'https://api.weibo.com/oauth2/authorize?' +
                    URI.encode_www_form(client_id: APP_ID,
                                        response_type: 'code',
                                        redirect_uri: "http://#{APP_HOST}/oauth_redirect"),
                method: :get %>

<% end %>


